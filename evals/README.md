# JobPilot system evaluations

Human and agent reviews of complete-pipeline accuracy.

| Path | What it is |
| --- | --- |
| `system/run3-system-accuracy.canvas.tsx` | **Run 3 accuracy canvas** (same as Cursor canvas) |
| `system/run-20260717T184341Z-accuracy.md` | Plain-language Run 3 review |
| `system/run-20260717T184341Z-accuracy.json` | Same review as structured JSON |

The interactive Cursor canvas still opens from the IDE canvases folder; this copy is the repo backup under `evals/system/`.

These files are **reviews of a run**, not the paid pipeline harness (that lives under `tests/evals/`).

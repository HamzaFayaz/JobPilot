# JobPilot Search Helper (local PC — install into worker/.venv only)
#
# Setup:
#   cd worker
#   python -m venv .venv
#   .venv\Scripts\activate        # Windows
#   pip install -r requirements.txt
#   copy .env.example .env
#
# Run (dev — not .exe):
#   .venv\Scripts\python.exe main.py
#
# Browser: Kimi WebBridge + Qwen ReAct loop (see System Design/kimi-webbridge-provider.md)
httpx>=0.27.0
openai>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
